# Discord Application client ID (public — visible in all OAuth URLs)
VITE_CLIENT_ID=your_discord_client_id_here

# Hostname for the API/WS proxy (no protocol, no trailing slash)
VITE_API_HOST=flightmap.cfod.co.uk
